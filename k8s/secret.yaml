# Sensitive configuration. In a real cluster this is created out-of-band
# (sealed-secrets / external-secrets / vault), never committed with real values.
apiVersion: v1
kind: Secret
metadata:
  name: transaction-api-secret
type: Opaque
stringData:
  FX_API_KEY: "replace-me-in-real-deployments"
